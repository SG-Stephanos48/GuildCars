@model GuildCars.UI.Models.UserEditViewModel

@{
    ViewBag.Title = "AddUser";
}

<div class="row">

    <div class="col-md-12">
        <h1>Add User</h1>
        <br />
        @using (Html.BeginForm("PostUser", "Admin", FormMethod.Post, new { id = "frmAddUser" }))
        {
            <div class="col-md-6">
                <div class="form-group">
                    <label>First Name</label>
                    @Html.TextBoxFor(m => m.User.FirstName)
                </div>
                <div class="form-group">
                    <label>Email</label>
                    @Html.TextBoxFor(m => m.User.Email)
                </div>
                <div class="form-group">
                    <label>Password</label>
                    @Html.TextBoxFor(m => m.User.PasswordHash)
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group">
                    <label>Last Name</label>
                    @Html.TextBoxFor(m => m.User.LastName)
                </div>
                <div class="form-group">
                    <label>Role</label>
                    @Html.DropDownListFor(m => m.User.RoleId, Model.Role, new { @class = "form-control" })
                </div>
                <div class="form-group">
                    <label>Confirm Password</label>
                    @Html.TextBoxFor(m => m.User.PasswordConfirmed)
                </div>
            </div>

            <div class="row">
                <div>
                    <button id="btnAddUser" class="btn btn-default">Save</button>
                </div>
            </div>
        }

    </div>
</div>